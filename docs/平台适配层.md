# cocos2d-x 3.0-oh 平台适配层

## 1. 概述

**文件位置**: `cocos/2d/platform/`, `cocos/platform/`

平台适配层为跨平台提供统一的接口抽象，封装不同操作系统的文件系统、图像处理、线程、设备信息等差异。

### 1.1 平台定义

**文件**: `cocos/base/CCPlatformConfig.h`

```cpp
// 平台标识
#define CC_PLATFORM_UNKNOWN            0
#define CC_PLATFORM_IOS                1
#define CC_PLATFORM_ANDROID            2
#define CC_PLATFORM_WIN32              3
#define CC_PLATFORM_MARMALADE          4
#define CC_PLATFORM_LINUX              5
#define CC_PLATFORM_BADA               6
#define CC_PLATFORM_BLACKBERRY         7
#define CC_PLATFORM_MAC                8
#define CC_PLATFORM_NACL               9
#define CC_PLATFORM_EMSCRIPTEN        10
#define CC_PLATFORM_TIZEN             11
#define CC_PLATFORM_QT5               12
#define CC_PLATFORM_WP8               13
#define CC_PLATFORM_WINRT             14
#define CC_PLATFORM_OHOS              15
```

### 1.2 平台目录结构
```
cocos/2d/platform/
├── CCCommon.h          # 平台通用工具
├── CCDevice.h          # 设备信息
├── CCFileUtils.h       # 文件系统工具
├── CCImage.h/cpp       # 图像处理
├── CCSAXParser.h       # XML SAX解析器
├── CCThread.h/cpp      # 线程封装
├── platform.cpp        # 平台实现
├── android/            # Android 特定
├── ios/                # iOS 特定
├── mac/                # macOS 特定
├── linux/              # Linux 特定
├── win32/              # Win32 特定
├── winrt/              # WinRT 特定
├── wp8/                # WP8 特定
└── ohos/               # OpenHarmony 特定
```

## 2. CCFileUtils - 文件系统工具

**文件**: `cocos/2d/platform/CCFileUtils.h`

```cpp
class FileUtils {
public:
    static FileUtils* getInstance();
    static void destroyInstance();
    
    // 路径管理
    virtual void addSearchPath(const std::string& path, bool front = false);
    virtual void setSearchPaths(const std::vector<std::string>& searchPaths);
    virtual const std::vector<std::string>& getSearchPaths() const;
    virtual std::string fullPathForFilename(const std::string& filename) const;
    virtual std::string fullPathFromRelativeFile(const std::string& filename,
                                                 const std::string& relativeFile) const;
    
    // 文件读取
    virtual std::string getStringFromFile(const std::string& filename);
    virtual Data getDataFromFile(const std::string& filename);
    virtual unsigned char* getFileData(const std::string& filename,
                                       const char* mode, ssize_t* size);
    virtual unsigned char* getFileDataFromZip(const std::string& zipFilePath,
                                              const std::string& filename,
                                              ssize_t* size);
    
    // 文件操作
    virtual bool isFileExist(const std::string& filename) const;
    virtual bool isAbsolutePath(const std::string& path) const;
    virtual bool createDirectory(const std::string& dirPath);
    virtual bool removeFile(const std::string& filepath);
    virtual bool removeDirectory(const std::string& dirPath);
    virtual bool renameFile(const std::string& path, const std::string& oldname,
                            const std::string& name);
    virtual bool renameFile(const std::string& oldfullpath, const std::string& newfullpath);
    
    // 目录信息
    virtual long getFileSize(const std::string& filepath);
    virtual std::string getFileExtension(const std::string& filePath) const;
    virtual std::string getWritablePath() const;
    
    // 资源路径
    virtual void setDefaultResourceRootPath(const std::string& path);
    virtual void setPopupNotify(bool notify);
    
    // 平台特定
    CC_DEPRECATED_ATTRIBUTE static void setResourceDirectory(const std::string& dir);
    std::string getSuitableFOpen(const std::string& filenameUtf8) const;
};
```

## 3. CCImage - 图像处理

**文件**: `cocos/2d/platform/CCImage.h`

```cpp
class Image {
public:
    enum class Format {
        JPG, PNG, TIFF, RAW_DATA, UNKNOWN
    };
    
    Image();
    ~Image();
    
    // 初始化
    bool initWithImageFile(const std::string& path);
    bool initWithImageData(const unsigned char* data, ssize_t dataLen);
    bool initWithRawData(const unsigned char* data, ssize_t dataLen,
                         int width, int height, int bitsPerComponent, bool preMulti = false);
    
    // 属性
    unsigned char* getData() const;
    ssize_t getDataLen() const;
    Format getFileType() const;
    int getWidth() const;
    int getHeight() const;
    int getBitsPerComponent() const;
    bool isPremultipliedAlpha() const;
    bool hasPremultipliedAlpha() const;
    
    // 保存
    bool saveToFile(const std::string& filename, bool isToRGB = true);
    
    // 缩放
    bool initWithImageFileThreadSafe(const std::string& fullpath);
    bool initWithImageDataThreadSafe(const unsigned char* data, ssize_t dataLen);
};
```

## 4. CCDevice - 设备信息

**文件**: `cocos/2d/platform/CCDevice.h`

```cpp
class Device {
public:
    enum class TextAlign {
        CENTER        = 0x33, ///< Horizontal center and vertical center.
        TOP           = 0x13, ///< Horizontal center and vertical top.
        TOP_RIGHT     = 0x12, ///< Horizontal right and vertical top.
        RIGHT         = 0x32, ///< Horizontal right and vertical center.
        BOTTOM_RIGHT  = 0x22, ///< Horizontal right and vertical bottom.
        BOTTOM        = 0x23, ///< Horizontal center and vertical bottom.
        BOTTOM_LEFT   = 0x21, ///< Horizontal left and vertical bottom.
        LEFT          = 0x31, ///< Horizontal left and vertical center.
        TOP_LEFT      = 0x11, ///< Horizontal left and vertical top.
    };

    static int getDPI();

    static void setAccelerometerEnabled(bool isEnabled);
    static void setAccelerometerInterval(float interval);

    static Data getTextureDataForText(const char* text,
                                      const FontDefinition& textDefinition,
                                      TextAlign align,
                                      int& width, int& height);
};
```

## 5. CCThread - 线程封装

**文件**: `cocos/2d/platform/CCThread.h`

```cpp
class ThreadHelper {
public:
    static void* createAutoreleasePool();
    static void releaseAutoreleasePool(void* autoreleasePool);
};
```

## 6. CCSAXParser - XML SAX解析器

**文件**: `cocos/2d/platform/CCSAXParser.h`

```cpp
class SAXDelegator {
public:
    virtual ~SAXDelegator() {}
    virtual void startElement(void* ctx, const char* name, const char** atts) = 0;
    virtual void endElement(void* ctx, const char* name) = 0;
    virtual void textHandler(void* ctx, const char* s, int len) = 0;
};

class SAXParser {
    SAXDelegator* _delegator;
public:
    SAXParser();
    ~SAXParser();

    bool init(const char* encoding);
    bool parse(const char* xmlData, size_t dataLength);
    bool parse(const std::string& filename);

    void setDelegator(SAXDelegator* delegator);

    static void startElement(void* ctx, const CC_XML_CHAR* name, const CC_XML_CHAR** atts);
    static void endElement(void* ctx, const CC_XML_CHAR* name);
    static void textHandler(void* ctx, const CC_XML_CHAR* name, int len);
};
```

## 7. CCCommon - 平台通用工具

**文件**: `cocos/2d/platform/CCCommon.h`

```cpp
// 语言枚举
enum class LanguageType {
    ENGLISH = 0,
    CHINESE,
    FRENCH,
    ITALIAN,
    GERMAN,
    SPANISH,
    DUTCH,
    RUSSIAN,
    KOREAN,
    JAPANESE,
    HUNGARIAN,
    PORTUGUESE,
    ARABIC,
    NORWEGIAN,
    POLISH,
    CHINESEHANT,
    VIETNAM
};

// 工具函数
void LuaLog(const char* format);
void MessageBox(const char* msg, const char* title);
```

## 8. OpenHarmony 平台特定实现

**文件位置**: `cocos/2d/platform/ohos/`

OHOS 平台提供以下特定实现：
- `CCGL.h` - OHOS OpenGL ES 绑定
- `CCStdC.h` - 标准C库适配
- 文件系统的 OHOS 特定路径处理
- `CCUserDefault-ohos.cpp` - OHOS持久化存储

## 9. 使用示例

### 9.1 文件读取
```cpp
// 获取文件工具单例
auto fileUtils = FileUtils::getInstance();

// 添加搜索路径
fileUtils->addSearchPath("res/");

// 获取文件完整路径
std::string fullPath = fileUtils->fullPathForFilename("image.png");

// 读取文件内容
std::string content = fileUtils->getStringFromFile("config.json");
```

### 9.2 图像加载
```cpp
auto image = new Image();
if (image->initWithImageFile("texture.png")) {
    int width = image->getWidth();
    int height = image->getHeight();
    auto format = image->getFileType();
    unsigned char* data = image->getData();
    // 使用图像数据...
}
CC_SAFE_DELETE(image);
```

### 9.3 设备信息
```cpp
int dpi = Device::getDPI();
Device::setAccelerometerEnabled(true);
Device::setAccelerometerInterval(1.0f / 30.0f);
```