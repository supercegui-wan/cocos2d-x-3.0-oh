# cocos2d-x 3.0-oh 编译构建实施计划

> 版本: 1.1  
> 日期: 2026-07-29  
> 更新: 2026-07-29（阶段一已完成）  
> 项目: cocos2d-x-3.0-oh  
> 目标: 恢复项目在 Win32 和 OHOS 平台的编译构建能力

---

## 1. 总体目标

分阶段恢复项目在各目标平台的编译构建能力，确保：
- Win32 (MSVC) 平台可编译、可运行、可调试 **【阶段一已完成】**
- OHOS 平台可交叉编译、可部署
- 所有可选模块（GUI、网络、物理、扩展等）均能成功编译

---

## 2. 分阶段实施计划

### 阶段一：Win32 预编译库恢复（P0）— **已完成**

**目标**: 恢复 Win32 平台缺失的预编译库，确保 MSVC 编译通过

**状态**: **已完成**（2026-07-29）

**实际完成情况**:

| 编号 | 任务 | 结果 | 验收 |
|------|------|------|------|
| 1.1 | 下载 GLFW 3.x Win32 预编译库 | 通过 MSYS2 mingw32 安装 `mingw-w64-i686-glfw`，用 `gendef` + `lib.exe` 生成 `.lib` | 通过 |
| 1.2 | 下载 GLEW Win32 预编译库 | 通过 MSYS2 mingw32 安装 `mingw-w64-i686-glew` | 通过 |
| 1.3 | 下载 libiconv Win32 预编译库 | 通过 MSYS2 mingw32 安装 `mingw-w64-i686-libiconv` | 通过 |
| 1.4 | 验证所有预编译库完整性 | 所有 DLL 文件已就位，`.REMOVED.git-id` 标记文件已清除 | 通过 |
| 1.5 | CMake 配置验证 | `cmake ..\.. -G "Visual Studio 12 2013"` 配置成功 | 通过 |
| 1.6 | 编译验证 | 15个静态库 + MH-Game.exe 编译成功，0 error | 通过 |
| 1.7 | 运行验证 | MH-Game.exe 正常启动，窗口显示，无断言失败 | 通过 |

**实际构建命令**:
```powershell
cd g:\cocos2d-x-3.0-oh
mkdir build\win32-msvc-vs2013-x86
cd build\win32-msvc-vs2013-x86
cmake ..\.. -G "Visual Studio 12 2013"
cmake --build . --config Debug --target MH-Game -- /m /v:minimal
```

**编译产物**:
```
build/win32-msvc-vs2013-x86/lib/Debug/
├── box2d.lib
├── chipmunk.lib
├── cocos2d.lib
├── cocosbase.lib
├── cocosbuilder.lib
├── cocostudio.lib
├── extensions.lib
├── kazmath.lib
├── network.lib
├── spine.lib
├── tinyxml2.lib
├── ui.lib
├── unzip.lib
├── websockets.lib
└── xxhash.lib

build/win32-msvc-vs2013-x86/bin/MH-Game/Debug/
├── MH-Game.exe (10.9 MB)
├── *.dll (30+ 运行时依赖)
└── Resources/ (自动复制)
```

**阶段一踩坑记录**：详见 [编译构建环境评估报告 - 踩坑记录](./编译构建环境评估报告.md#10-踩坑记录关键问题与解决方案)，关键问题包括：
- 踩坑 #1: 预编译库缺失恢复
- 踩坑 #2: libwebsockets 编译错误（LWS_LIBRARY_VERSION 宏）
- 踩坑 #3: 启动断言失败（config-example.plist 找不到）
- 踩坑 #4: 启动黑屏（setSearchPaths 覆盖搜索路径）
- 踩坑 #5: VS Code IntelliSense 报错
- 踩坑 #6: PDB 文件写入失败
- 踩坑 #7: Git Submodule 错误

---

### 阶段二：CMake 与工具链升级（P1）

**目标**: 升级 CMake 到正式版，评估 VS2013 替代方案

**时间**: 预计 1 天

**任务清单**:

| 编号 | 任务 | 详情 | 验收标准 |
|------|------|------|---------|
| 2.1 | 升级 CMake 到 3.28+ 正式版 | 下载安装 CMake 3.28+，替换 3.10.0-rc1 | `cmake --version` 显示 3.28+ |
| 2.2 | 添加 VS2022 构建支持 | 验证 CMake 能否生成 VS2022 解决方案 | CMake 生成成功 |
| 2.3 | 配置 x64 构建目标 | 添加 64 位编译配置 | 64 位编译通过 |
| 2.4 | 更新 .gitignore | 排除编译产物目录（build-*/） | build 目录不被 git 跟踪 |

**风险**:
- VS2013 到 VS2022 的 C++ 标准变化可能导致编译问题
- 预编译库需要重新编译为 x64 版本

---

### 阶段三：OHOS 编译环境搭建（P0）

**目标**: 安装 OHOS 开发工具链，实现 OHOS 平台交叉编译

**时间**: 预计 2-3 天

**任务清单**:

| 编号 | 任务 | 详情 | 验收标准 |
|------|------|------|---------|
| 3.1 | 安装 DevEco Studio | 从华为开发者官网下载安装 | DevEco Studio 正常运行 |
| 3.2 | 安装 OHOS SDK | 通过 SDK Manager 下载 Native SDK | `$OHOS_SDK_HOME/native/` 目录存在 |
| 3.3 | 安装 OHOS NDK | 通过 SDK Manager 下载编译工具链 | `aarch64-linux-ohos-g++` 可调用 |
| 3.4 | 配置环境变量 | 设置 `OHOS_SDK_HOME`、`OHOS_NDK_HOME` | 环境变量生效 |
| 3.5 | 编写 CMake 工具链文件 | 为 OHOS 创建 `cmake/ohos.toolchain.cmake` | 工具链文件语法正确 |
| 3.6 | CMake 配置验证 | 使用 OHOS 工具链执行 CMake 配置 | CMake 配置无错误 |
| 3.7 | 编译验证 | 执行 OHOS 交叉编译 | 编译零错误，生成 OHOS 二进制 |
| 3.8 | 预编译库验证 | 确认 `external/*/prebuilt/ohos/` 目录完整 | 所有 OHOS 预编译库存在 |

**风险**:
- OHOS NDK 版本与预编译库的 ABI 兼容性
- DevEco Studio 需注册华为开发者账号
- 缺少真机或模拟器进行运行时验证

---

### 阶段四：Android 编译验证（P1）

**目标**: 验证现有 Android 工具链能否正常编译

**时间**: 预计 1 天

**任务清单**:

| 编号 | 任务 | 详情 | 验收标准 |
|------|------|------|---------|
| 4.1 | 验证 NDK 版本兼容性 | 确认 NDK r16b 与项目代码兼容 | 无编译器版本警告 |
| 4.2 | CMake 配置 | 使用 Android 工具链执行 CMake 配置 | CMake 配置无错误 |
| 4.3 | 编译验证 | 执行 Android 编译 (armeabi-v7a) | 编译零错误 |
| 4.4 | APK 打包验证 | 使用 Gradle 或 Ant 打包 APK | 生成可安装的 APK |

**风险**:
- NDK r16b 较旧，gradle 插件版本可能不兼容
- 需要配置 Android SDK 的 build-tools 版本

---

### 阶段五：完整构建验证与 CI 配置（P2）

**目标**: 确认所有目标平台可编译，建立自动化构建流程

**时间**: 预计 2 天

**任务清单**:

| 编号 | 任务 | 详情 | 验收标准 |
|------|------|------|---------|
| 5.1 | Win32 Debug/Release 全量编译 | 编译所有模块 + 测试项目 | 编译零错误 |
| 5.2 | Win32 运行测试 | 运行 MH-Game 所有测试用例 | 主要测试用例通过 |
| 5.3 | OHOS 全量编译 | 编译所有模块 | 编译零错误 |
| 5.4 | 编写构建脚本 | 创建 `scripts/build_win32.bat`、`scripts/build_ohos.sh` | 一键构建 |
| 5.5 | GitHub Actions CI 配置 | 创建 `.github/workflows/build.yml` | CI 自动触发构建 |
| 5.6 | 构建产物归档 | 配置 CI 自动上传构建产物 | Release 包含编译产物 |

**风险**:
- GitHub Actions 的 Windows runner 可能缺少某些依赖
- OHOS 编译在 CI 环境中需要特殊配置

---

## 3. 任务依赖关系

```
阶段一（Win32 预编译库） ──→ 阶段二（CMake 升级） ──→ 阶段五（全量验证）
         │
         └──→ 阶段四（Android 验证） ──→ 阶段五（全量验证）

阶段三（OHOS 环境） ──────────────────→ 阶段五（全量验证）
```

阶段一和阶段三可并行进行，阶段四依赖阶段一（环境基础验证），阶段五依赖所有前置阶段。

---

## 4. 角色与职责

| 角色 | 职责 | 阶段 |
|------|------|------|
| 构建工程师 | 预编译库恢复、CMake 配置、编译调试 | 阶段一、二、五 |
| OHOS 开发工程师 | DevEco Studio 安装、OHOS 工具链配置、OHOS 编译 | 阶段三 |
| Android 开发工程师 | Android 编译验证、APK 打包 | 阶段四 |
| DevOps 工程师 | CI/CD 配置、构建脚本编写 | 阶段五 |

---

## 5. 验收标准汇总

| 阶段 | 关键验收标准 | 状态 |
|------|------------|------|
| 阶段一 | `MH-Game.exe` 在 Windows 上正常启动并显示渲染窗口 | **已通过** |
| 阶段二 | CMake 3.28+ 能生成 VS 解决方案，编译无错误 | 待执行 |
| 阶段三 | 生成 OHOS 平台可执行文件，`file` 命令确认为 ARM64 OHOS 格式 | 待执行 |
| 阶段四 | 生成 Android APK，可在模拟器或真机安装 | 待执行 |
| 阶段五 | GitHub Actions 上 CI 构建通过，构建产物可下载 | 待执行 |

---

## 6. 里程碑时间线

```
Week 1:  ████████ 阶段一（Win32 预编译库恢复）✅ 已完成
Week 1:  ████████ 阶段三（OHOS 环境搭建）[并行]
Week 2:  ████     阶段二（CMake 升级）
Week 2:  ████     阶段四（Android 验证）
Week 2-3: ████████ 阶段五（全量验证 + CI）
```

---

## 7. 构建产物清单

| 产物 | 平台 | 格式 | 用途 |
|------|------|------|------|
| `libcocos2d.lib` | Win32 | 静态库 | 游戏开发链接 |
| `MH-Game.exe` | Win32 | 可执行文件 | 功能验证 |
| `libcocos2d.a` | OHOS | 静态库 | 游戏开发链接 |
| `libcocos2d.so` | OHOS | 动态库 | 运行时加载 |
| `libcocos2d.a` | Android | 静态库 | 游戏开发链接 |
| APK | Android | 安装包 | 设备安装测试 |

---

## 8. 附录：环境变量参考

### 8.1 需配置的环境变量

```powershell
# Win32 构建
$env:VS120COMNTOOLS = "D:\Program Files (x86)\Microsoft Visual Studio 12.0\Common7\Tools\"

# Android 构建
$env:ANDROID_HOME = "D:\Android\android-sdk-64"
$env:ANDROID_NDK_HOME = "D:\Android\android-sdk-64\ndk\16.1.4479499"
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_144"

# OHOS 构建（待安装后配置）
$env:OHOS_SDK_HOME = "<DevEco Studio SDK 路径>"
$env:OHOS_NDK_HOME = "<OHOS Native NDK 路径>"

# CMake 工具链
$env:CMAKE_HOME = "D:\Program Files\CMake"
```

### 8.2 预编译库目录结构（修复后）

```
external/
├── glfw3/prebuilt/win32/
│   └── glfw3.lib          ← 恢复
├── win32-specific/
│   ├── gles/prebuilt/
│   │   ├── glew32.dll     ← 恢复
│   │   └── glew32.lib     ← 恢复
│   └── icon/prebuilt/
│       └── iconv.dll      ← 恢复
└── chipmunk/prebuilt/ohos/
    └── libchipmunk.a      ← 已存在
```